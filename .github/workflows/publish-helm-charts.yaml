name: Publish Helm Charts to GitHub Pages

on:
  push:
    paths:
      - 'charts/**'
      - '.github/workflows/publish-helm-charts.yaml'

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: 🧾 Checkout Repo
        uses: actions/checkout@v3

      - name: 🧰 Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: v3.13.0

      - name: 🧼 Clean Old Docs
        run: |
          mkdir -p docs
          rm -f docs/*.tgz
          rm -f docs/index.yaml

      - name: 📦 Package Charts
        run: |
          cd charts
          helm package dashy
          helm package media-stack
          mv *.tgz ../docs/

      - name: 🗂️ Generate Helm Repo Index
        run: |
          helm repo index docs --url https://jpcortesp.github.io/jp-helm-repos/docs

      - name: 🚀 Commit & Push Changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/
          git commit -m "Auto-update Helm charts and index via GitHub Actions" || echo "No changes to commit"
          git push
